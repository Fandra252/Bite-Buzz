import { COLOR } from "./Color";
import { BORDER_RADIOUS } from "./borderRadious";
import { FONT_WEIGHT } from "./fontWeight";
import { FONT_SIZE } from "./fontSize";
import { LINE_HEIGHT } from "./lineHeight";
import { SPACING } from "./spacing"; 
import { SIZE_PERCENT, SIZE_PX, SIZE_VIEWPORT } from "./size";
import { BORDER } from "./border";
import { TRANSITION } from "./transition";
import { SHADOW } from "./shadow";

export const theme = {
  COLOR,
  BORDER_RADIOUS,
  FONT_WEIGHT,
  FONT_SIZE,
  LINE_HEIGHT,
  SPACING,
  SIZE_PERCENT,
  SIZE_PX,
  SIZE_VIEWPORT,
  BORDER,
  TRANSITION,
  SHADOW,
};

export default theme;
